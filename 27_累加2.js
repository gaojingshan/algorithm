// 第5题 【累加2】计算表达式 2 + 22 + 222 + 2222 + 22222 + 222222 + 2222222 + 22222222 + 222222222 的值。要求使用循环语句。
// 在前一项基础上加上 2乘以10的 i 次方
//sum所有数累加和 , count计算每个式子
for (var i = 1, sum = 2, count = 2; i <= 8; i++) {
  count += 2 * Math.pow(10, i);
  sum += count;
}
console.log(`值为${sum}`);
