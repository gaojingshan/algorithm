// 第2题 【判断质数】寻找100以内的所有质数。
// i遍历100以内的数
for (var i = 1; i <= 100; i++) {
    // 遍历1到当前i的数
  for (var j = 1, sum = 0; j <= i; j++) {
    if (i % j == 0) {
      sum++;
    }
  }
  if (sum == 2) {  //当前i能被整除的数等于2时,是质数
    console.log(i);
  }
}
